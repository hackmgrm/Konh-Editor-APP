# 更新记录

## 1.0.2 · 2026-08-31 · 空核编辑器定制版

### 品牌升级

- 应用英文名由 Mars Editor 改为 KonhEditor，中文名改为“空核编辑器”。
- 预览、长图导出和品牌署名统一为“空核域界”。
- npm、Rust、Tauri 产品名与应用标识同步更新。
- README、主题说明、设置页和窗口标题同步更新品牌文案。

### 内置主题

- 从 `isjiamu/gzh-design-skill` 适配 5 套主题：摸鱼绿、红白色系、石墨极简、留白禅意、摸鱼票据。
- 从 `Kianzzz/zhouxing-paiban-wx` 适配 5 套主题：石墨档案、绿白清简、墨蓝刊读、雾紫叙事、沙金手记。
- 内置主题总数扩展至 23 套。
- 原“橄榄手记”改为“空核域界”品牌定制主题；内部 ID 保留 `olive-journal`，兼容旧草稿设置。
- 主题选择改为按文章保存：不同文章可分别使用不同主题，切换文章时自动恢复。
- 文章重命名或移动后保留主题关联，删除文章时同步清理关联记录。

### 空核域界品牌版式

- 支持 Markdown 顶部 front-matter 自动生成品牌头图、导语卡、摘要标签和文末签名卡。
- 修复右侧预览遗漏 front-matter 头图、显示“未命名文章”的问题。
- 品牌头图启用时隐藏预览器的默认文章头，避免标题和日期重复。
- 旧稿中的 `author: 空运新视角` 自动迁移显示为“空核域界”，其他明确署名保持不变。

### 图标

- 新增空核域界品牌图标：以空心核心、交汇航线和向上航向表达航空货运、跨界连接与新视角。
- 同步替换 macOS、Windows、iOS、Android 和网页 favicon 图标资源。
- 图标进一步简化为“笔记页 + 空心核心 + 青色编辑笔划”，并同步到软件左上角品牌标记。

### 界面外观

- 支持浅色、深色和“跟随系统”三种模式。
- 默认跟随系统，并监听操作系统外观变化，在应用运行中自动切换夜间模式。
- 预览右上角增加“手机 / 桌面”手动切换，并在本机记住选择。

### API Agent

- 支持配置 OpenAI 兼容 Chat Completions 接口，包括 Base URL、API Key 和模型名。
- 设置页支持连接测试；API Key 只保存在本机应用配置目录。
- Agent 新增 API 模式，可在当前工作区内列出、搜索、读取、创建和修改文本文件。
- API Agent 不启动本地终端，并拒绝绝对路径、上级路径及任何越出工作区的文件访问。
- Agent 面板移除 Claude/Codex 切换，只保留 API 工作方式，并提供醒目的“API 配置”入口。
- API 请求不再显式传递 `tool_choice`，兼容 DeepSeek 思考模型的工具调用限制。
- Agent 输入框左下角改为模型快速切换菜单，无需返回设置中心。

### 更新渠道

- 应用内更新检测、下载页和 `latest.json` 更新源切换到 `hackmgrm/Konh-Editor-APP`。
- Release 标题、Windows 构建产物名称及项目链接同步为空核编辑器归属。
- 更新签名公钥替换为空核编辑器自有密钥，后续安装包不再信任原作者的更新签名。
- Release 工作流增加 macOS 构建，与 Windows、Linux 一并生成自有更新包。

### 验证

- `npm run build` 通过。
- `cargo check --manifest-path src-tauri/Cargo.toml` 通过。
- Tauri macOS Apple Silicon `.app` 与 `.dmg` 打包通过（未签名）。
